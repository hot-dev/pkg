::resend::integration::emails ns
meta ["test"]

// =============================================================================
// Integration Tests
// =============================================================================
// These tests require RESEND_API_KEY to be set in context
// For send tests, also set RESEND_TEST_EMAIL

test-resend-api-connection
meta ["test"]
fn () {
    // Test basic API connectivity by listing API keys (read-only)
    keys ::resend::api/keys/list()

    assert(keys.data, "Response should have 'data' field")
}

test-list-domains
meta ["test"]
fn () {
    domains ::resend::domains/list()

    assert(domains.data, "Response should have 'data' field")
}
